import { Router } from "express";

// Search is mounted under boards in board.routes.js
// This file exists for standalone use if needed
// GET /api/v1/boards/:boardId/search is handled in board.routes.js

const router = Router();

export default router;
